# DGX Spark unified memory — prevent OOM lockups
# CUDA allocations are pinned and cannot be swapped.
# Default swappiness=60 causes a death spiral: swap thrashing + frozen I/O.

vm.swappiness = 10
vm.oom_kill_allocating_task = 1
vm.min_free_kbytes = 524288
vm.watermark_scale_factor = 50
